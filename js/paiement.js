import {clamp,clampOperation,isInteger,resetInputNum} from "./common.mjs";

setInterval(()=>{
    var index = 0;
    while(index < cardNum.value.length){
        if(true){

        }
        index++;
    }
},100);